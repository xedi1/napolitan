'use client';

/**
 * Placeholder icons for PWA
 * Since we can't generate PNGs directly, we use inline SVGs as data URIs
 */
export function PWAPlaceholderIcons() {
  // This component doesn't render anything visible
  // It just ensures the icons are referenced for the manifest
  return null;
}
